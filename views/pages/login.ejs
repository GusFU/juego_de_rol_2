<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head.ejs'); %>
</head>

<body>
    <header>
        <div class="Developer">
            <nav class="nav">
                <div class="planetExpress">
                    <img class="imgPlanet" src="logo.png" />
                </div>

                <div class="Nombrelogo">
                    <p class="logo">
                        <span><strong>Planet Express</strong></span>
                    </p>
                </div>

                <div class="register">

                    <a href="./registro"><b>Registro</b></a>
                </div>
                <div class="tracking">
                    <a href="./tracking"><b>Seguimiento</b></a>
                </div>
                
                <div class="welcome">

                    <div class="welcome">
                        <a href="./landingPage" id="nameD"><b>Bienvenido</b></a>
                    </div>
                </div>
            </nav>
    </header>


    <main>
        <form action="/login" method="POST">
            <div class="formulario">
                <%   try{if (registrado) {%>
                    <p id="mensajeCorrecto"><b>Registrado correctamente</b></p>
                <% } }catch (e){console.log(e)} %>
                <fieldset id="formularioM">

                    <legend>INICIO DE SESIóN</legend>


                    <div class="contenedor-campos">


                        <div class="campo">
                            <label>Email</label>
                            <input id="emailLoginS" type="email" placeholder="Introduce tu email Ej:02sergioconde@gmail.com" name="emailus"
                             pattern="^[a-zA-Z0-9_\-\.~]{2,}@[a-zA-Z0-9_\-\.~]{2,}\.[a-zA-Z]{2,4}$">
                        </div>

                        <div class="campo">
                            <label>Contraseña</label>
                            <input id="passwordLoginS" type="password" placeholder="Introduce tu contraseña Ej:Aa1-sergio"
                                name="contrasenaus" pattern= pattern="^(?=^.{6,}$)((?=.*\d)(?=.*[A-Z])(?=.*[a-z])|(?=.*\d)(?=.*[^A-Za-z0-9])(?=.*[a-z])|(?=.*[^A-Za-z0-9])(?=.*[A-Z])(?=.*[a-z])|(?=.*\d)(?=.*[A-Z])(?=.*[^A-Za-z0-9]))^.*">
                                
                        </div>
                        <style type="text/css" media="screen">
                            #mensajeAlerta{
                                color: red;
                                       }
                            #mensajeCorrecto{
                                color: rgb(43, 209, 43);
                            }
                        </style>
                        <%   try{if (!loginmal) {%>
                            <p id="mensajeAlerta"><b>Email o contraseña incorrectos</b></p>
                        <% } }catch (e){console.log(e)} %>


                    </div>
                    <div class="enviar">
                        <input type="submit" class="boton" value="Log In"></input>
                        <br><br>
                    </div>

            </div>
            </fieldset>
        </form>

    </main>







    <footer class="footer-distributed">

        <%- include('../partials/footer'); %>
    </footer>
</body>

</html>